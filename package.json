{
    "name": "fanxie-wp-updates-dev",
    "private": true,
    "devDependencies": {
        "@wordpress/env": "^10.0.0"
    },
    "scripts": {
        "env:start": "wp-env start",
        "env:stop": "wp-env stop",
        "test:integration": "wp-env run tests-cli --env-cwd=wp-content/fx-updates-repo vendor/bin/phpunit --testsuite Integration",
        "worker:test": "npm --prefix worker test"
    }
}
